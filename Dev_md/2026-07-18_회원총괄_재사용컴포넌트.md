# 2026-07-18 www 회원 총괄 — 사이트 쪽 재사용 컴포넌트 (원본)

## 한 일
- 신규 사이트에 파일 복사만으로 쓰는 재사용 표준 3종 생성 (사이트 고유 값은 SITE_ID 상수 하나):
  - `src/lib/supabaseClient.ts` — env 우선 + 공용 URL/anon key fallback 하드코딩(§3), `SITE_ID`(`[사이트id]` 플레이스홀더), `WwwProfile` 타입.
  - `src/auth/LoginButtons.tsx` — 구글+카카오 2종(§3 기본 탑재). OAuth 시작 전 `localStorage['signup_site']=SITE_ID`, `signInWithOAuth({ redirectTo: origin })`.
  - `src/auth/OnboardingGate.tsx` — www_profiles 조회, is_complete=false면 온보딩 폼으로 전 화면 차단(라우터 무의존). 필수 3종(이름·연락처 010-0000-0000 형식·이메일, auth email 초기값) + 선택 2종(소속·과정). 저장 시 signup_site null이면 localStorage 값 기록 후 키 삭제. insert 정책이 없어 update만(행 생성은 가입 트리거 전담 — 스키마 확정본 유지).
- 사용법 문서: `docs/auth-onboarding-guide.md` (복사 3파일 + SITE_ID 수정 + App 감싸기), README에 재사용 표준 섹션 추가.

## 검증
- `npm ci` + `npm run build`(tsc -b strict + vite build) 통과 — 신규 3파일 포함 오류 0건.

## 이유
- 2026-07-18 대표 확정: 전 사이트 가입 시 기본 정보 입력 + 가입 출처 기록, www 총괄 관리. DB(www_profiles 체계)는 선행 완료.

## 남은 일
- 각 사이트 적용은 사이트별 작업 시 개별 진행(일괄 적용 금지 지시).
- www 쪽 관리 화면은 06-personal/www `/admin/members`로 별도 커밋.
